// Shared input styling so every field primitive looks identical. One place to
// change border/focus treatment instead of per-component class strings.
export const inputClass =
  "w-full rounded-xl border border-field bg-white px-3 py-2.5 text-sm text-ink outline-none transition-colors focus:border-brand-500";

export const labelClass = "mb-2 block text-sm font-semibold text-ink";

export const helpClass = "mt-2 text-xs text-ink-soft";
